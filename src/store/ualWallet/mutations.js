export function setAuthenticator (state, authenticator) {
  state.authenticator = authenticator
}

export function setAccount (state, account) {
//   console.log('setAccount state', state)
//   console.log('setAccount acc', account)
  state.account = account
}
